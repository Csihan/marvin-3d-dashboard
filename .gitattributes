# ============================================================
# .gitattributes —— 跨平台行尾与二进制资产规范（开源模板标配）
# @file    .gitattributes
# @brief   统一仓库内行尾为 LF、二进制资产不做行尾转换,保证跨平台贡献者 diff 干净
# @author  Csihan
# @date    2026-09-08
#
# 说明（为什么强制 LF）:
#   - Windows 贡献者本地检出可经 core.autocrlf 转为 CRLF,但仓库内统一 LF,
#     避免 Linux CI 与 macOS 贡献者看到 "假改动";
#   - 二进制文件（图片/PDF/模型）标 binary,防止任何行尾/编码转换损坏文件。
# ============================================================

# 文本源码:入库统一 LF
* text=auto eol=lf

# Windows 专属脚本保留 CRLF(.bat 在 LF 下部分解析器会出错)
*.bat text eol=crlf
*.cmd text eol=crlf

# 二进制资产:禁止行尾/编码转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.pdf binary
*.stl binary
*.glb binary
*.zip binary
