# ============================================================
# .npmrc —— 项目级 npm 配置（公开仓库模板）
# @file    .npmrc
# @brief   可选：把特定 scope 指向自建私有 npm 源（示例已全部注释，默认走公网源）
# @author  Csihan
# @date    2026-09-08
#
# 说明（为什么默认全注释）：
#   - 本仓库的依赖（vue/three/urdf-loader 等）全部来自公共 npm 源，
#     开箱即用，不需要任何额外配置；
#   - 如果你的团队也采用「scope 指向私有源」的分发模式（例如用 Verdaccio
#     在内网托管自己的组件包），可以参考下面被注释的写法启用：
#
# @myscope:registry=https://your-registry.example.com
# //your-registry.example.com/:_authToken=${NPM_TOKEN}
#
#   - 把 @myscope / your-registry.example.com 换成你的 scope 与私服地址；
#   - token 通过环境变量 NPM_TOKEN 注入，切勿把真实 token 提交进仓库；
#   - 公共依赖不建议走私服，保持默认源即可。
# ============================================================
